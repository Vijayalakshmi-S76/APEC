<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>APEC • Login</title>
  <style>
    :root { --bg:#f5f7fb; --card:#ffffff; --text:#1f2937; --muted:#6b7280; --primary:#2563eb; }
    * { box-sizing: border-box; }
    body {
      margin: 0; min-height: 100vh; display: grid; place-items: center;
      background: radial-gradient(1200px 800px at 20% 10%, #eef2ff 0, transparent 60%), var(--bg);
      font: 16px/1.4 system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      color: var(--text);
    }
    .card {
      width: min(420px, 92vw);
      background: var(--card);
      border-radius: 20px;
      box-shadow: 0 12px 30px rgba(0,0,0,.08);
      padding: 28px;
    }
    .logo {
      display: grid; place-items: center; margin-bottom: 18px;
    }
    .logo img {
      width: 84px; height: 84px; object-fit: contain;
      border-radius: 16px; background: #eef2ff; padding: 8px;
    }
    h1 { font-size: 1.25rem; margin: 6px 0 18px; text-align: center; }
    form { display: grid; gap: 14px; }
    label { font-size: .9rem; color: var(--muted); display: block; margin-bottom: 6px; }
    input[type="text"], input[type="password"] {
      width: 100%; padding: 12px 14px; border: 1px solid #e5e7eb; border-radius: 12px;
      background: #fff; outline: none; transition: border .15s, box-shadow .15s;
    }
    input:focus {
      border-color: var(--primary);
      box-shadow: 0 0 0 4px rgba(37,99,235,.12);
    }
    .btn {
      display: inline-block; width: 100%;
      padding: 12px 16px; border: 0; border-radius: 12px;
      background: var(--primary); color: #fff; font-weight: 600; cursor: pointer;
    }
    .hint { text-align: center; font-size: .85rem; color: var(--muted); margin-top: 8px; }
  </style>
</head>
<body>
  <main class="card" role="main" aria-labelledby="title">
    <!-- APEC logo field -->
    <div class="logo" aria-hidden="true">
      <!-- Replace with your logo file or URL -->
      <img src="apec-logo.png" alt="APEC logo">
    </div>

    <h1 id="title">APEC Login</h1>

    <form action="#" method="post" autocomplete="on">
      <div>
        <label for="username">Username</label>
        <input id="username" name="username" type="text" placeholder="Enter username" required />
      </div>

      <div>
        <label for="password">Password</label>
        <input id="password" name="password" type="password" placeholder="Enter password" required />
      </div>

      <button class="btn" type="submit">Login</button>
    </form>

    <p class="hint">Need help? Contact your administrator.</p>
  </main>
</body>
</html>
